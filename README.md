# turtlebot3_acmpc

Research package for training TurtleBot3 navigation using AC-MPC (Actor-Critic Model Predictive Control) with PPO.

## Development Status

**Step 1.1: Project Initialization** — ✅ Complete

### Completed

- `src/` layout with module stubs
- `pyproject.toml` with dependencies (PyTorch, CasADi, Gymnasium, etc.)
- `bump-my-version` for semantic versioning
- `LICENSE` (MIT)
- `.gitignore` with proper exclusions
- `tests/conftest.py` with base fixtures
- Development docs moved to `dev_info/`

### Deferred

- `package.xml` — ROS 2 integration to be finalized later
- CLI entry point — Step 8.1
- Gazebo/Docker setup — Step 1.2

## Next Step

**Step 1.2** — Docker and Gazebo Setup
